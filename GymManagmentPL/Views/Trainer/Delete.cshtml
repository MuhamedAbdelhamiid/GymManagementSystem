
<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card p-5 shadow text-center">

                <p class="fs-3 text-primary-color mb-4">Are you sure you want to delete @ViewBag.TrainerName ?</p>

                <div class="alert alert-warning text-start" role="alert">
                    <strong>Warning!</strong> This action cannot be undone. All Trainer data will be permanently deleted.
                </div>

                <form asp-action="DeleteConfirmed" method="post" class="mt-4" asp-route-id = "@ViewBag.TrainerId">
                    <div class="buttons d-flex justify-content-between gap-3">
                        <a  asp-action="Index" class="btn-outline flex-fill">No</a>
                        <button type="submit" class="btn-fill flex-fill">Yes</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>